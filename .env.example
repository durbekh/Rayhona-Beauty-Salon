# Rayhona Salon Environment Variables
# Copy this file to .env.local and fill in your actual values

# Admin Authentication Credentials
# WARNING: Never commit real credentials to version control!
NEXT_PUBLIC_ADMIN_USERNAME=user
NEXT_PUBLIC_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD

# Google Apps Script URL for data saving
# Get this URL from your Google Apps Script deployment
NEXT_PUBLIC_GOOGLE_SCRIPT_URL=

# Instructions:
# 1. Copy this file: cp .env.example .env.local
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit .env.local to version control (it's in .gitignore)
# 4. For production, set these as environment variables in your hosting platform
